.row
  .offset4.span4.step1-content
    h2
      | Step 2: Grab the code and Go!
    p.spaced
      | You should see a line of images below if everything is working (give it a second)
    div.proxigram-photo-list
    p.spaced
      | If you want to use the simple jQuery plugin, copy and paste the following code:
    blockquote
      = "<script type=\"text/javascript\" src=\"\" ></script>"

if (app.settings.env == "development")
  endpoint = "http://proxigram.dev/api/photos";
else
  endpoint = "http://proxigram.com/api/photos";

script(type='text/javascript')
  jQuery(document).ready(function($){
    $('.proxigram-photo-list').proxigram({api_endpoint: "!{endpoint}", limit: 5, access_token: "!{current_user["api_key"]}"})
  });